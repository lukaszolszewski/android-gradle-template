buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:0.7.0'
    }
}

apply plugin: 'idea'
apply plugin: 'eclipse'

task wrapper(type: Wrapper) {
    gradleVersion = '1.9'
}